\documentclass[margin=1mm]{standalone}

\input{../tikz}
\input{../font}
\input{../tableau_colors}

%\pgfdeclarelayer{background}
%\pgfdeclarelayer{foreground}
%\pgfsetlayers{background,foreground}

\begin{document}

\begin{tikzpicture}
  %
  \def\n{5}
  %
%%  \def\radiusi{1.25cm}
%%  \def\radiusj{3.5cm}
%%  \def\radiusk{6.25cm}
%%  \def\radiusl{8.5cm}
%%
  \def\radiusl{2.5cm}
  \def\radiusk{4.0cm}
  \def\radiusj{7.25cm}
  \def\radiusi{9.5cm}

  %
%  \def\marginl{25} % margin in angles, depends on the radius
%  \def\margink{25}  % margin in angles, depends on the radius
%  \def\marginj{12}  % margin in angles, depends on the radius
%  \def\margini{3}  % margin in angles, depends on the radius
  %

  \node[draw, circ, very thick, fill=black , fill opacity = 1, minimum size=2*\radiusi] at (0,0) {};
  \node[draw, circ, very thick, fill=dred  , fill opacity = 1, minimum size=1.45*\radiusj] at (0,0) {};
  \node[draw, circ, very thick, fill=black , fill opacity = 1, minimum size=0.45*\radiusk] at (0,0) {};
  \node[draw, circ, very thick, fill=dred  , fill opacity = 1, minimum size=\radiusl,font=\bfseries, align=center] at (0,0) {Probabilistic \\ Moments};
  
%  \foreach \s in {1,...,\n}{
%    \node[draw, circ, very thick, fill=white!25, minimum size=1cm] at ({360/\n * (\s - 1)} : \radiusi){$y_\s$};
%  }

  %
  \foreach \s in {1,...,\n}{
    \coordinate (a) at ({360/\n * (\s - 1)} : \radiusj);
    \node[draw, circ, very thick, fill=white!25,  minimum size=1cm, align=center] at (a) {
      $ \left\langle R\left(y,u(y) \right) \Big| \widehat{\psi_\s}(y) \right\rangle $ \\ $=$ \\ $0$
      %$ \left\langle R\left(y,u(y) \right) \Big| \widehat{\psi_\s}(y) \right\rangle =  0 $
    };

  } 
  %
  \foreach \s in {1,...,\n}{
    \node[draw, circ, very thick, fill=white!25, minimum size=1cm,align = center] at ({360/\n * (\s - 1)} : 0.8*\radiusk){
      $ \left\langle F\left(y,u(y) \right) \Big| \widehat{\psi_\s}(y) \right\rangle $ 
      %$F(y_\s,u(y_\s))$
      \\  $\&$\\
      $ \left\langle \frac{dF}{d\xi}(y,u(y)) \Big| \widehat{\psi_\s}(y) \right\rangle $ 
      %$\frac{dF}{d\xi}(y_\s,u(y_\s))$
    };
  }
  %
%  \foreach \s in {1,...,\n}{
%    \node[draw, circ, very thick, fill=white!25] at ({360/\n * (\s - 1)} : \radiusl){$\alpha_\s$};
%  }
\end{tikzpicture}

\end{document}
