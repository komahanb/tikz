\documentclass[margin=1mm]{standalone}

\input{../tikz}
\input{../font}
\input{../tableau_colors}

\begin{document}

\begin{tikzpicture}
  %
  \def\n{5}
  %
  \def\radiusi{1.25cm}
  \def\radiusj{3.5cm}
  \def\radiusk{6.5cm}
  \def\radiusl{8.5cm}
  %
  \def\margini{30} % margin in angles, depends on the radius
  \def\marginj{30}  % margin in angles, depends on the radius
  \def\margink{15}  % margin in angles, depends on the radius
  \def\marginl{5}  % margin in angles, depends on the radius
  %
  \foreach \s in {1,...,\n}{
    \node[draw, circ, very thick, minimum size=1cm] at ({360/\n * (\s - 1)} : \radiusi){$y_\s$};
    \draw[-, >=latex, very thick] ({360/\n * (\s - 1) + \margini} : \radiusi) 
    arc ({360/\n * (\s - 1) + \margini} : {360/\n * (\s)-\margini} : \radiusi);
  }
  %
  \foreach \s in {1,...,\n}{
    \node[draw, circ, very thick, minimum size=2cm] at ({360/\n * (\s - 1)} : \radiusj){$R(y_\s,u(y_\s))=0$};
    \draw[-, very thick, >=latex] ({360/\n * (\s - 1) + \marginj} : \radiusj) 
    arc ({360/\n * (\s - 1) + \marginj} : {360/\n * (\s)-\marginj} : \radiusj);
  }
  %
  \foreach \s in {1,...,\n}{
    \node[draw, circ, very thick, minimum size=2cm] at ({360/\n * (\s - 1)} : \radiusk){$F(y_\s,u(y_\s))$};
    \draw[-, very thick, >=latex] ({360/\n * (\s - 1) + \margink} : \radiusk) 
    arc ({360/\n * (\s - 1) + \margink} : {360/\n * (\s)-\margink} : \radiusk);
  }
  %
  \foreach \s in {1,...,\n}{
    \node[draw, very thick, circ] at ({360/\n * (\s - 1)} : \radiusl){$\alpha_\s$};
    \draw[-, very thick, >=latex] ({360/\n * (\s - 1) + \marginl} : \radiusl) 
    arc ({360/\n * (\s - 1) + \marginl} : {360/\n * (\s)-\marginl} : \radiusl);
  }
\end{tikzpicture}

\end{document}
